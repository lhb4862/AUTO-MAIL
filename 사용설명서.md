# AUTO-MAIL 사용 설명서

정해진 **요일·시간**에 메일을 자동 발송하는 Windows용 프로그램입니다.  
설정은 **AUTO-MAIL 설정** 창에서 하며, 코드나 JSON을 직접 수정할 필요가 없습니다.

---

## 1. 준비물

| 항목 | 설명 |
|------|------|
| Windows PC | Windows 10 이상 권장 |
| Python 3 | [python.org](https://www.python.org/downloads/) 설치 시 **"Add Python to PATH"** 체크 |
| 인터넷 | SMTP 메일 발송용 |

---

## 2. 최초 1회 설치

다운로드받은 `AUTO-MAIL` 폴더 안에서 **우클릭** → **터미널에서 열기**(또는 **PowerShell에서 열기**)를 선택한 뒤, 아래를 실행합니다.

```
pip install -r requirements.txt
```

> 처음 실행 시 `config.json`, `.env` 파일이 자동 생성됩니다.  
> 비밀번호·수신자 정보가 들어가므로 **GitHub 등에 올리지 마세요.**

---

## 3. 프로그램 실행 (설정 창 열기)

- **`Auto Mail Set.vbs`** 더블클릭 → 설정 창이 열립니다.

---

## 4. 설정 저장

발송 시간·요일·메일 내용·SMTP 정보를 입력한 뒤, 화면 오른쪽 아래 **저장** 버튼을 누릅니다.

### 저장할 때

1. **저장** 클릭 → "변경된 내용을 저장하시겠습니까?"에서 **예** 선택
2. 설정 창에 **"설정을 저장했습니다"** 안내가 뜨면 완료입니다

> 저장 시 **오류 메시지**가 뜨면 내용을 확인하세요. 오류 코드가 함께 표시됩니다.

> **PC가 켜져 있어야** 설정한 요일·시간에 자동 발송됩니다.  
> 정해진 시간에 메일을 보낼 때 **터미널 창은 뜨지 않습니다.**

---

## 5. SMTP 설정 (예시 Gmail)

Gmail은 **일반 로그인 비밀번호로 SMTP 발송을 허용하지 않습니다.**  
반드시 **앱 비밀번호**를 사용해야 합니다.

### 앱 비밀번호 만드는 방법

1. [Google 계정](https://myaccount.google.com/) 로그인
2. **보안** → **2단계 인증** 켜기
3. **앱 비밀번호** 메뉴 이동
4. 앱: **메일**, 기기: **Windows 컴퓨터** (또는 기타) 선택 후 생성
5. 표시된 **16자리 비밀번호**를 설정 창 **SMTP 비밀번호**에 입력 (공백 없이)

### 설정 창 입력 예 (Gmail)

```
보내는 사람 (From) : mymail@gmail.com
SMTP 계정          : mymail@gmail.com
SMTP 비밀번호      : (앱 비밀번호 16자리)
SMTP 서버          : smtp.gmail.com
SMTP 포트          : 587
```

### 자주 나는 SMTP 오류

| 오류 메시지 | 원인 | 해결 |
|-------------|------|------|
| `535 BadCredentials` | 비밀번호 오류 | 앱 비밀번호 재확인 |
| `534 Application-specific password required` | 일반 비밀번호 사용 | 앱 비밀번호로 변경 |
| `Connection refused` / 타임아웃 | 서버·포트 오류 | `smtp.gmail.com`, 포트 `587` 확인 |

Gmail 외 SMTP(네이버, 회사 메일 등)를 쓰는 경우 해당 메일 서비스의 SMTP 주소·포트·인증 방식을 확인하세요.

---

## 6. 발송 기록 확인

발송 결과는 **엑셀 파일**로 저장됩니다.

```
logs/mail_log.xlsx
```

엑셀에서 아래 순서로 확인할 수 있습니다.

| 발송시간 | 보내는사람 메일 | 받는사람 | 결과 | 오류내용 |
|----------|-----------------|----------|------|----------|

- **결과**: `성공`, `실패`, `테스트`, `발송 안 함` 등
- **오류내용**: 실패했을 때 **원인**만 간단히 기록됩니다. (예: 비밀번호 오류, 서버 연결 실패)

### 로그가 안 보일 때

- **`mail_log.xlsx`를 엑셀로 열어둔 상태**에서는 파일이 잠겨 새 기록이 바로 안 보일 수 있습니다.  
  **엑셀을 닫은 뒤** 다시 열거나, 아래 임시 파일을 확인하세요.
- 발송 직후 엑셀이 열려 있었다면 기록이 임시로 저장됩니다:

```
logs/mail_log_pending.jsonl
```

- 엑셀을 닫고 **다음 발송이 한 번 더 실행**되면, 임시 기록이 `mail_log.xlsx`로 합쳐집니다.

발송 실패 시 이 파일의 **마지막 행**을 먼저 확인하세요.
